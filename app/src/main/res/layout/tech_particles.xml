<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- חלקיק 1 - אייקון קוד -->
    <ImageView
        android:id="@+id/particle1"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:src="@drawable/ic_code_particle"
        android:alpha="0.4"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="50dp"
        android:layout_marginTop="100dp" />

    <!-- חלקיק 2 - אייקון שרת -->
    <ImageView
        android:id="@+id/particle2"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/ic_server_particle"
        android:alpha="0.5"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginEnd="80dp"
        android:layout_marginTop="150dp" />

    <!-- חלקיק 3 - אייקון לולאה -->
    <ImageView
        android:id="@+id/particle3"
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:src="@drawable/ic_loop_particle"
        android:alpha="0.3"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="100dp"
        android:layout_marginBottom="200dp" />

    <!-- חלקיק 4 - אייקון דאטה -->
    <ImageView
        android:id="@+id/particle4"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:src="@drawable/ic_data_particle"
        android:alpha="0.4"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginEnd="70dp"
        android:layout_marginBottom="180dp" />

    <!-- חלקיק 5 - אייקון ענן -->
    <ImageView
        android:id="@+id/particle5"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:src="@drawable/ic_cloud_particle"
        android:alpha="0.6"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="50dp" />

</androidx.constraintlayout.widget.ConstraintLayout> 